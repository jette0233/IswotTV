Place the production TLS certificate chain at `fullchain.pem` and its private key at
`privkey.pem`, or set `TLS_CERT_PATH` and `TLS_KEY_PATH` in the root `.env` file.
Certificate and key files are intentionally ignored by Git.

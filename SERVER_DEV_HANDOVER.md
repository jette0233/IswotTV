# 课堂签到助手项目交接文档

> 适用场景：项目从本地开发迁移到生产机服务器持续开发，供后续 P2 及以后阶段接手、维护、迭代使用。

---

## 1. 项目概述

本项目是一个面向学习通场景的**全自动托管签到助手**，当前已完成 **P1 阶段核心功能**，后续 **P2 及以后的开发将直接在生产机服务器上进行**。

当前系统核心目标：

- 由**生产者**负责扫码获取签到二维码中的 `enc`
- 由**消费者守护进程**自动监听课程并执行签到
- 支持课程管理、课程成员管理、签到日志记录、后台管理与课程接管
- 对普通二维码签到、定位签到已跑通
- 对防作弊验证码签到已做过深入逆向与工程处理，目前结论已明确

---

## 2. 当前技术架构

### 2.1 总体架构

当前项目采用：

- **后端**：Flask
- **前端**：Vue3 + Vite + Element Plus
- **数据库**：MySQL
- **消息/状态流转**：Redis Stream
- **自动签到守护**：Flask 进程内嵌 consumer daemon
- **图像处理**：OpenCV / ddddocr（历史尝试过）

### 2.2 当前架构原则

项目当前明确坚持以下原则：

1. **不引入额外复杂度**
   - 不拆成一堆独立服务
   - 不额外引入多端口
   - 不再为了“看起来规范”而过度工程化

2. **Flask 持久进程内集成 consumer 守护线程**
   - 当前已经这么做了
   - 后续服务器开发也默认延续这个模式，除非明确要重构

3. **Redis Stream 按 course_id 分桶**
   - 用于生产者推送 enc 和消费者消费
   - 有 TTL / 时效控制逻辑

4. **生产者选举制**
   - 一个课程同一时刻只应有一个有效生产者
   - 通过心跳维持主生产者身份

---

## 3. 当前已完成功能（P1 完成情况）

### 3.1 用户与认证

已完成：

- 用户注册
- 用户登录
- 登录态返回 uid
- 登录态支持管理员状态识别
- 前台用户体系可正常使用

### 3.2 课程系统

已完成：

- 创建课程
- 查询课程列表
- 修改课程
- 删除课程
- 退出课程
- 课程成员管理
- 课程教师字段支持（`teacher_name`）
- 创建者识别（`is_creator`）
- 成员可见性/权限控制

当前权限规则：

- **创建课程**：任何前台用户
- **查看课程**：创建者 / 课程成员 / admin
- **修改课程**：创建者 / admin
- **删除课程**：创建者 / admin
- **退出课程**：普通成员可退出；创建者逻辑需按当前后端规则执行

### 3.3 生产者机制

已完成：

- 生产者认领课程
- 生产者心跳保活
- 生产者上传 `enc`
- 浏览器端摄像头扫码识别二维码
- 前端页面承担生产者功能，不再依赖独立 Python 扫码脚本

当前原则：

- 一个课程同一时刻只能有一个活跃生产者
- 心跳超时后允许其他人重新竞选

### 3.4 消费者自动签到

已完成：

- 后台 consumer daemon 持续扫描签到任务
- 自动读取课程队列中的签到信息
- 自动执行签到
- 支持定位签到
- 支持标准二维码签到
- 记录签到日志

当前运行方式：

- consumer 守护线程已集成在 Flask 后端进程内
- 当前默认扫描周期约 **5 秒**
- 在服务器部署时，必须确认 Flask 进程长期稳定运行，否则 consumer 也会一起停掉

### 3.5 防作弊验证码签到相关结论

这部分结论非常关键，后续不要再重复走弯路。

#### 已确认结论

1. 学习通验证码相关接口存在严格限制
2. `captcha.chaoxing.com` 的 `check` 端点存在同源保护
3. 无法通过普通 Python 请求或跨域 fetch 稳定完成自动验证
4. 历史上尝试过 OpenCV / ddddocr / Canny / matchTemplate 等方案
5. 最终结论是：

> **短期内不应再把“防作弊验证码自动求解”作为可交付功能。**

#### 当前工程处理方式

- 若课程被识别为需要验证码：
  - 将课程标记为 `has_captcha = true`
  - 生产者接口拒绝继续推送
  - 消费者自动跳过该课程
  - 前端/日志中提示需人工处理

#### 当前态度

- **工程上先规避，不硬攻**
- 如果未来要继续深挖：
  - 方向是深度学习暴力采样 / 坐标映射建模
  - 该方向更像研究课题，而不是短期工程需求

---

## 4. 管理后台体系

### 4.1 后台入口

当前已实现独立后台入口：

- **前端路由**：`/txjadmin`
- **后端路由前缀**：`/api/txjadmin`

这是独立后台管理体系，不是普通前台页面。

### 4.2 后台已实现功能

已完成：

- 仪表盘
- 用户列表
- 用户搜索 / 分页
- 课程总览
- 课程详情
- 课程成员查看
- 踢出课程成员
- 课程接管
- 验证码开关管理
- 签到日志查询

### 4.3 课程详情与成员管理

后台课程详情页目前支持：

- 查看课程基础信息
- 查看课程创建者
- 查看课程教师
- 查看课程成员列表
- 查看成员加入时间
- 踢出成员

返回字段已覆盖：

- `uid`
- `nickname`
- `phone`
- `is_creator`
- `joined_at`

### 4.4 课程接管逻辑

已实现“后台管理员接管课程”。

#### 当前约定（必须记住）

**前台 user 表中的 `is_admin = true`，表示该账户是后台 admin 的代理账户。**

说白了就是：

- 后台 admin 是独立登录体系
- 但前台 `users` 表里会有一个被绑定的代理账号
- 只要 `users.is_admin = true`，就代表它和后台 admin 是绑定的

#### 因此，当前系统口径为：

- **前台 admin 代理账户** 和 **后台 admin 管理员身份** 是绑定关系
- 后续涉及：
  - 课程接管
  - 管理员识别
  - 特权判断
- 一律按这个约定处理

#### 课程接管执行结果应为：

- 把课程创建者改为 `is_admin = true` 的那个前台代理账户
- 同时保证课程成员表中，这个代理账户是课程成员且为创建者

---

## 5. admin 账户约定（非常重要）

这是后续服务器开发必须遵守的口径，避免再次混乱。

### 5.1 当前事实

当前允许存在两种“admin”含义：

#### 1）前台 user 表里的 admin
- 存在于 `users` 表
- 可以昵称叫 `admin`
- 可以手机号叫 `admin`
- 如果 `is_admin = true`，则表示它是后台 admin 的代理账户

#### 2）后台超级管理员 admin
- 用于 `/txjadmin` 登录
- 属于独立后台登录体系
- 密码可与前台 admin 不一致

### 5.2 统一约定

后续统一口径如下：

> **只要前台 `users.is_admin = true`，就视为该账号与后台 admin 绑定。**

不要再把这块拆成“完全无关的两套 admin”去写逻辑，否则会继续乱。

---

## 6. 数据库与字段现状

当前重要字段包括但不限于：

### users 表
- `id`
- `nickname`
- `phone`
- `password`
- `is_admin`

### courses 表
- `id`
- `name`
- `teacher_name`
- `has_captcha`
- `creator_id`
- 其他课程相关字段

### course_members 表
用于课程成员关系管理，至少承担：
- 成员归属
- 创建者标识
- 加入时间

### sign_logs 表
用于记录签到执行情况与结果

---

## 7. 当前已知部署现状（本地阶段）

当前本地已验证通过的环境：

- **MySQL**：`localhost:3306/chaoxing`
- **Redis**：`localhost:6379`
- **Flask**：`http://10.110.247.25:5000`
- **前端开发**：Vite 代理 `/api` 到 Flask

说明：

- 当前这些信息属于**本地已跑通状态**
- 上服务器后，这些地址、账号、进程启动方式要重新按服务器环境确认

---

## 8. 服务器开发阶段的交接要求

你接下来要把项目直接扔到服务器上开发，这意味着开发模式会发生变化。

### 8.1 后续开发默认环境

后续 P2 开发默认以**生产机服务器环境**为准：

- 开发、调试、修 bug 都在服务器上完成
- 不再假设本地环境是最终主环境
- 本地最多作为临时编辑或备份环境

### 8.2 服务器开发时必须优先关注的问题

#### 1）进程稳定性
因为 consumer daemon 是挂在 Flask 进程里的，所以：

- Flask 挂了 = 自动签到也挂了
- 服务器上必须确保后端进程长期稳定运行
- 后续最好配好：
  - `systemd` / `supervisor` / `pm2` 这类守护方式之一
  - 日志输出
  - 自动重启

#### 2）数据库真实结构
服务器环境里必须重新确认：

- `users.is_admin`
- `courses.has_captcha`
- `courses.teacher_name`

这些关键列是否真的在库里存在。
不要只看模型，要看**实际数据库结构**。

#### 3）Redis 可用性
要确认：

- Redis 是否启动
- Flask 是否能连通 Redis
- Stream key 是否正常生成
- TTL / 心跳 / 队列消费是否正常

#### 4）前后端地址问题
服务器开发后，必须统一：

- 前端访问域名
- 后端 API 域名
- HTTPS
- 反向代理
- 跨域 / Cookie / 登录态问题

#### 5）日志能力
服务器开发阶段不要再靠“猜”。

必须能快速看到：

- Flask 日志
- consumer 签到日志
- Redis 异常
- 数据库报错
- 路由 404 / 500

### 8.3 推荐的服务器侧目录与运维原则

建议后续服务器上至少保持以下清晰分层：

- 项目代码目录
- Python 虚拟环境目录
- 日志目录
- 配置文件目录
- 前端构建产物目录

原则：

1. **配置与代码分离**
2. **日志落盘**
3. **环境变量统一管理**
4. **启动命令固定**
5. **每次改动后明确重启哪个服务**

---

## 9. 后续 P2 开发建议重点

当前 P1 已经把骨架立起来了，P2 更值得做的是这些：

### 9.1 服务器稳定化
- 后端守护
- Redis / MySQL 连接稳定性
- 自动重启
- 异常恢复
- 日志系统

### 9.2 权限与后台细化
- 后台课程接管逻辑再做健壮性校验
- 前后台 admin 行为边界再收口
- 用户与课程成员权限边界继续打磨

### 9.3 前端易用性
- 管理后台细节体验
- 课程操作提示
- 错误提示更清楚
- 状态可视化

### 9.4 签到链路可靠性
- enc 时效问题
- 定位参数稳定性
- 消费重试策略
- 幂等控制
- 失败分类

---

## 10. 当前明确不要走的弯路

以下是已经踩过坑、后续不要反复重来的点：

1. **不要再指望验证码 check 接口可轻松自动化**
2. **不要把后台 admin 和前台 user 关系写乱**
3. **不要只看模型就认定数据库字段一定存在**
4. **不要让旧 Flask 进程继续跑，导致新路由没生效**
5. **不要把系统拆得过度复杂**
6. **不要把生产环境开发继续当本地开发那样随便重启和试错**

---

## 11. 交接结论

当前项目状态可以概括为：

### 已完成
- P1 主体可用
- 前后端主流程已跑通
- 管理后台已成型
- 自动签到主链路已具备
- 课程管理与成员管理已具备
- admin 绑定关系已明确

### 当前核心约定
- **`users.is_admin = true` 即后台 admin 的前台代理账户**
- 前后台 admin 是绑定关系，后续都按这个口径执行

### 后续重点
- 项目迁移到服务器持续开发
- 服务器成为主开发环境
- P2 重点放在稳定性、运维、权限细化、链路可靠性
